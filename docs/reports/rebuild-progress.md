# 连续重建进度

更新时间：2026-09-14

状态值：未开始／执行中／通过／失败／等待条件。生成文件存在不等于质量通过。

| 任务 | 状态 | 证据 | 下一步 |
| --- | --- | --- | --- |
| T00 基线与样片 | 部分完成 | `rebuild-T00.md`；服务和 GPU 检查已记录 | 保留缺失录像，继续 T01/T02 |
| T01 真实状态、计时、模式说明 | 部分完成 | `Job` 已记录实际 quick/full 阶段毫秒数，增加 `validation_status=not_run`；测试通过 | 继续把真实视觉质检结果接入状态 |
| T02 公共查看器 | 部分完成 | `frontend/src/App.tsx` 已移除跟随镜头原图兜底层；前端构建通过；按钮顺序经浏览器树确认 | 真实场景仍需固定路线录像和颜色对比 |
| T03 候选工具实测 | 失败/阻塞 | `rebuild-T03.md`；GPU审计确认 RTX 4050 Laptop 6141 MiB；四个候选未安装，未下载权重，未上传照片；TripoSR接近显存上限，Hunyuan3D完整流程超预算 | 等待独立 D 盘环境、网络和权重条件；不接入主页面，继续无模型任务 |
| T04 室内结构 | 执行中/待视觉 | `rebuild-T04-corridor-browser-2026-09-14.md`；新增 `context_shell.py` 室内墙地壳层，走廊快速样片 `026dcc3a251a4002a98aef72c6766477`；前端对壳层材质启用双面渲染 | 复测走廊与第二张室内照片的侧向、回头和移动；若出现廉价平面或穿模，关闭默认壳层 |
| T05 自然地形 | 执行中/待视觉 | `rebuild-T05-snow-browser-2026-09-14.md`；新增 `terrain_horizon_shell`，雪山快速样片 `378c55c10b554544819b38496205a1c7`；另删除极端长条面 6,445 个；远景明确标为程序化实验 | 复测雪山与第二张自然照片的侧向、飞行和回头；不能把蓝色天空壳层称为真实山谷 |
| T06 场景契约与移动 | 部分完成 | `rebuild-T06.md`；生成后写入 `quality_metrics`，机器检查与视觉验收分离；26项后端测试通过；质量检查已修正 GLB 节点变换测量 | 需固定路线视觉验收；不能以机器检查代替走入、绕行、回头录像 |
| T07 街道与建筑 | 部分完成/等待条件 | `rebuild-T07.md`；新增 `benchmark_urban.py`，街道和建筑立面各一张真实样片的线索 JSON 与叠加图 | 仍需 T03 主体分离候选及俯拍下降/沿立面飞行录像；不得把线段证据当作城市场景通过 |
| T08 主体互动 | 部分完成/等待条件 | `rebuild-T08.md`；新增 `benchmark_subject_actions.py`，动作区域、素材状态、非目标点击和冷却约束可检查 | 仍需真实猫/人物动作素材与背景稳定录像；摄像头手势继续暂缓 |
| T09 完整版与渐进 | 部分完成/失败候选 | `rebuild-T09.md`；新增 `compare_scene_versions.py`，雪山 quick/full 的覆盖率不升且 p99 变差已被比较门捕获 | 先接入真实新增结构/表面步骤，再做同一路线和切换位置视觉验收 |
| T10 导出性能 | 部分完成 | 离线包测试通过；分享包不再包含 `source.jpg`，仅带场景与查看器；新增 `verify_export.py` 内容完整性检查 | 需第二台电脑断网打开并记录加载耗时 |

当前执行点：T04/T05 仍是执行中/待视觉，旧场景侧向、回头、飞行存在明显撕裂/裸洞风险，不能标通过。T07/T08/T09 已完成不依赖显卡的前置检查：城市线索、主体动作契约和 quick/full 比较门已加入；T10 已完成两个导出包的本机完整性检查。下一步需要朋友的独立 GPU/模型环境补做主体与真实结构，用户或第二台电脑补做浏览器路线、断网导出和帧率验证。绝不把脚本输出或包完整性误报为 3D 重建通过。
