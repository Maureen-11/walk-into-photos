# T02 公共查看器修正

执行日期：2026-09-14

## 已完成

- 移除 `PhotoBackdrop`：覆盖率低时不再把原照片固定到相机前方，避免原图与破碎网格重叠造成“浆糊”观感。
- 移除 `useTexture` 依赖和“原图保真兜底层”标签。原图仍可作为上传预览和独立对比资料，不参与场景渲染。
- 将 MoGe 结果文案改为诚实表述：可见区域投色；不可见区域目前只是深度估计候选几何，未完成纹理补全。
- 体验模式顺序已固定为：快速体验、完整体验、渐进体验（推荐）。

## 验收

- 后端：`19 passed in 2.62s`。
- 前端：`pnpm run build` 通过；仅有 bundle size 警告。
- 浏览器可访问性树确认三个按钮顺序正确；截图确认当前界面不再显示原图兜底标签。

## 尚未通过

- 没有把雪山、街道或楼道的网格质量标成通过；本任务只移除错误的渲染补救，不会自动修复几何。
- 固定路线录像、颜色并排对比和第二台 RTX 4060 验证仍待完成。
