# T08 主体互动契约前置检查

执行日期：2026-09-14

## 本次完成

- 新增 `backend/scripts/benchmark_subject_actions.py`。
- 检查主体区域是否存在、是否在归一化画布内、冷却时间是否合理、动作素材是否真的存在。
- 明确非目标点击为 no-op，重复点击在冷却结束前阻止；`experimental` 或 `unverified` 永远不能被脚本升级为 `available`。
- 新增两个单元测试，覆盖未验收素材和越界区域。

## 验收结果

| 子项 | 状态 | 说明 |
| --- | --- | --- |
| 动作清单结构 | 部分完成 | 已能输出逐动作检查报告 |
| 猫/人物局部动作 | 等待条件 | 没有通过视觉验收的局部动作素材 |
| 背景稳定与动作连续性 | 未完成 | 仍需真实浏览器录像，不能用整图抖动代替 |
| 摄像头手势 | 暂缓 | 不进入本轮 |

## 结论

T08 的安全契约已补齐，但主体互动本身不能标为完成。当前主界面继续显示“素材待验收”，这符合真实状态；待朋友或独立环境提供可复现动作素材后，再运行局部运动与背景稳定的视觉验收。
