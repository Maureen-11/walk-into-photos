# 修正阶段 4：quick/full 坐标连续性

日期：2026-09-14
状态：协议修正完成，full 的视觉增益和切换录屏尚未通过。

完整版生成现在读取同一任务的 quick manifest，并复用 quick 的 `world_scale`；不会因为更高输入分辨率而另起一套归一化坐标。quick 失败时不盲猜，full 仍使用自己的尺度并保留未验证状态。

这只保证坐标协议的前提，不等于 full 已经有纹理补全或质量改善。当前 full 仍是“候选”，需要在相同位置、相同朝向进行对比，若只有分辨率变化而没有可见改善，不升级状态。

后端测试 `19 passed`，前端构建成功。
