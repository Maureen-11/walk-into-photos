# T04/T05 旧场景机器基线

执行日期：2026-09-14

本轮没有把旧 MoGe 网格冒充新结构或地形引擎，只用机器检查提前暴露风险。

## 检查命令

```text
backend\\.venv\\Scripts\\python.exe scripts\\benchmark_scene_geometry.py --scene <scene.glb> --manifest <manifest.json> --json <runtime-json>
```

## 结果

| 类别 | 旧场景 | 三角形 | 覆盖率 | 机器判断 |
| --- | --- | ---: | ---: | --- |
| 雪山 | `f0025ea8...` | 418,794 | 0.5788 | `needs_visual_review` |
| 室内 | `a95fbf88...` | 2,441,092 | 1.0 | `needs_visual_review` |
| 街道 | `6a8e823e...` | 614,464 | 1.0 | `needs_visual_review` |

三个网格顶点均为有限值、退化三角形比例为0；这只能证明文件结构可读，不能证明表面连续、墙线笔直、人物不扭曲或可以绕行。机器检查已输出范围、边长和三角形长宽比，结果 JSON 在 `walk-runtime-data`，不进 Git。

另外用用户提供的真实样片运行了无模型证据分析：

- 雪山：地平线候选约在 `y=0.5715`，近景带从 `y=833` 开始；输出 `terrain-snow-2026-09-14.json` 和叠加图。它仍只是地形分层线索，不是连续山谷地形。
- 走廊：检测到 85 条候选线、30 条近垂直线，消失点候选约为 `[343.7, 295.5]`；输出 `indoor-corridor-2026-09-14.json` 和叠加图。它仍不是墙面、地面或碰撞体。
- 夜间楼房样片也完成了同一结构分析，但它不应被误标为室内可行走场景，后续应走建筑立面模板。

## 结论

T04/T05 尚未通过。下一次要加入结构／地形实验模块和固定路线录像；继续使用统一 MoGe 网格不能称为完成。
