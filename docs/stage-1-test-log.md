# 阶段 1 测试记录

此文件只记录已在当前分支重新运行的结果。旧的在线 DeepSeek 记录不再作为参赛版证据。

## 当前验证（2026-09-13）

- 后端 Python compileall：通过。
- 规划单元测试：4 passed。
- 前端 `pnpm run build`：通过。
- mock API 冒烟：上传、损坏图片 400、分析、任务轮询、manifest、GLB 和离线 ZIP 导出均通过；当前入口不需要邀请码。
- SQLite：分析结果和任务 JSON 可写入并重新读取。
- 前端交互：拖拽上传、模板选择、快速/完整/渐进模式选择、360°环顾、WASD、F飞行、空格/C垂直移动已接通。
- MoGe v2：四张参赛样片的真实 GPU 快速 GLB 均已生成，覆盖率均为 0.9；尚未完成逐类视觉验收。
- Moondream 2B：官方源码、transformers 依赖和约3.85GB权重已缓存到 D 盘；断网分类结果为雪山→natural_landscape、街道→street_city、猫咪→animal_closeup、夜间楼房→architecture，模型释放测试通过。
- 真实配置端到端：雪山上传后离线 Moondream 返回 200，创建 quick 任务，真实 MoGe 任务轮询到 QUICK_READY，manifest/scene_id 可查询。

## 2026-09-13 复核更正

- 本轮再次确认雪山、街道、猫样片可由本地规划和 MoGe 生成 GLB；未将其误记为可走体验通过。
- 无 mask 时覆盖率现在为未知值，不再使用旧的固定 0.9。
- 主体回应、地面碰撞、路线录屏和跨电脑离线打开仍待真实验收；代码级进度见 `docs/reports/vnext-execution-status-2026-09-13.md`。

## 尚未通过的阶段门

- 四张参赛样片全部分类正确并成功生成。
- 真实模式下山谷前进、街区下降、猫咪微缩、建筑立面飞行的视觉质量截图/视频。
- 6GB 显卡峰值显存和完整模式耗时记录。
- 另一台电脑断网打开导出包并确认自包含查看器（当前导出仍是 GLB + manifest ZIP）。

## 真实性边界

统一入口、模板规划、状态持久化和基础查看器的自动化链路通过，不等同于八类场景已经完成。照片不可见区域仍须标注为 AI 估计或程序化创作，任何未经过样片和视觉验收的模板不得标记为完成。
