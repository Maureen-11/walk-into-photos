# 交接验证与待验证边界

本轮整理交接文档、忽略规则、开发测试依赖和源码打包脚本。没有更换生成算法，没有删除原机运行资产，没有推送 GitHub。

本轮重新执行：后端32项测试通过；前端构建通过（619模块，存在约1MB主包体积警告）。这不等于新电脑首次安装成功，也不是视觉效果验收。

打包采用允许列表，只带应用代码、脚本、测试、配置样例、锁文件和 Markdown 文档。PACKAGE_MANIFEST.json 包含每个文件 SHA256。ZIP 检查逐文件存在；排除 .env、数据库、照片、权重、运行目录和第三方源码。

文档检查：三份专用文档均存在；HANDOFF 链接可定位；原话仅摘录项目工作沟通。旧报告保留历史价值，其中机器路径或缺少的本地证据不是可以在新机器直接使用的资源。

重点更正：T07/T08/T09 脚本没有接入所有真实行为门禁；主体区域点击/重复点击尚未实测；独立版本对比不能直接认定渐进升级通过；导出 file:// 尚未通过。接手者需要新增实测证据才能修改这些状态。
